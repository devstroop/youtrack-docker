# YouTrack Configuration
YOUTRACK_VERSION=2025.2.93511
YOUTRACK_PORT=8080

# YouTrack Volume Paths
# Leave empty to use Docker named volumes (recommended)
# Or specify absolute paths for bind mounts (requires proper permissions)
# Examples:
#   YOUTRACK_DATA=/var/lib/youtrack/data
#   YOUTRACK_DATA=./data
YOUTRACK_DATA=
YOUTRACK_CONF=
YOUTRACK_LOGS=
YOUTRACK_BACKUPS=

# S3 Configuration
AWS_S3_BUCKET=your-bucket-name
AWS_S3_ACCESS_KEY_ID=your-access-key
AWS_S3_SECRET_ACCESS_KEY=your-secret-key
AWS_S3_URL=https://s3.us-east-1.amazonaws.com

# S3FS Mount Options (optimized for performance and reliability)
# - allow_other: Allow other users to access mounted directory
# - use_cache: Enable local cache for better performance
# - max_stat_cache_size: Cache more metadata
# - stat_cache_expire: Cache expiration in seconds (900s = 15min)
# - multireq_max: Parallel requests for better throughput
# - parallel_count: Number of parallel multipart upload threads
S3FS_ARGS=-o allow_other -o use_cache=/tmp/cache -o max_stat_cache_size=100000 -o stat_cache_expire=900 -o multireq_max=5 -o parallel_count=10